import { TabSkeleton } from "../_components/TabSkeleton";

/** The configuration tab: the game's form, its configs, the danger zone. */
export default function Loading() {
  return <TabSkeleton blocks={3} />;
}
